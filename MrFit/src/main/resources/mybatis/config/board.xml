<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
 	<!--[정현][공지사항 갯수 확인]  -->
 		<select id="getTotalNoticeCount">
 			select count(*) from board where category='공지'
 		</select>
 	
 	<!--[정현][공지사항 리스트 받기]  -->
 	<select id="noticeList">
 	 SELECT rnum,id,title,content,regdate,security,category 
		from (select row_number() over(order by bno desc) as rnum,bno,id,title,content,regdate,security,category  from board where category='공지')
		board
		where rnum between #{startRowNumber} and #{endRowNumber}
		ORDER BY bno DESC  
 	</select>
    
</mapper>










